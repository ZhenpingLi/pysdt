"""
SDT Configuration Management.

This package handles the loading, parsing, and access of application-wide 
and satellite-specific configuration settings. It provides a centralized 
mechanism to retrieve parameters such as database paths, algorithm defaults, 
and operational thresholds from JSON files.

The core module, :mod:`sdt_config`, manages the active satellite context 
and provides utility functions for type-safe retrieval of configuration values.
"""
